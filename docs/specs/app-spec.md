# ESL Learning Assistant 仕様書（v1）

策定経緯: [docs/plans/archive/app-spec.md](../plans/archive/app-spec.md)

## 1. 概要

アメリカで ESL（English as a Second Language）の授業を受ける学習者が、授業中・授業後に使う
iPhone アプリ。教科書を撮影して文字起こし・翻訳し、知らない単語を単語帳に登録し、
教科書内容から練習問題を自動生成する。

## 2. 想定ユーザー

- アメリカで ESL クラスを受講している学習者
- 日本語ネイティブに限らず、母語が異なる学習者も想定する
  - UI／翻訳先言語は学習者が設定画面で指定した母語を使う（日本語固定にしない）

## 3. コア機能

### 3.1 撮影 → 文字起こし・翻訳（OCR & Translation）

- 教科書ページなどをカメラで撮影する（撮影自体はオフラインでも可能）
- 撮影した画像を Claude API に送信し、文字起こし（OCR）と翻訳を行う（オンライン時のみ）
- 文字起こし結果と翻訳結果を並べて表示する

### 3.2 単語帳（Vocabulary）

- 単語の登録方法
  - OCR結果のテキストをタップして登録
  - 自由入力で手動登録
- 単語ごとに保持する情報
  - 翻訳
  - 例文（教科書からの抜粋、または AI 生成）
  - 発音（TTS による音声読み上げ）
  - 品詞・文法情報
- 復習機能（間隔反復の復習クイズ）を持つ
  - 間隔反復は固定ステップの Leitner 方式（3日→7日→14日→30日→90日。正解で次ステップ、
    不正解で最初のステップに戻す）。詳細は [data-model.md](data-model.md) §5 `WordReviewState`
  - `dueDate <= 今日` の単語を「今日の復習」として Words タブから出題する
  - 問題は28形式（テキスト・音声・イラスト出題 × 4択・タイプ入力回答、問題文はすべて英語）。
    **backend が AI 生成してサーバに保存**し（1単語×1形式につき複数バリエーション）、
    アプリは復習セッション開始時にまとめて取得して、その中からランダムに出題する。
    サーバに問題が無い単語（オフライン・生成前）は出題しない。
    形式一覧は [docs/plans/archive/word-memorization-quiz.md](../plans/archive/word-memorization-quiz.md) §3.3、
    サーバ生成・保存の設計は
    [docs/plans/archive/quiz-questions-server-storage.md](../plans/archive/quiz-questions-server-storage.md)
  - AI 生成問題（3.3章、レッスン紐づけ）とは別機能。復習クイズは `Question` レコードを作らず、
    結果は単語の復習状態（`reviewState`）の更新のみで記録する

### 3.3 問題作成（Quiz Generation）

- 撮影・登録した教科書内容を元に、Claude API で練習問題を自動生成する
- 出題形式: 選択肢（多肢選択）問題／穴埋め問題／記述・並べ替え問題
- 難易度調整 UI は持たず、教科書内容に応じた難易度を AI に一任する
- 演習結果（正答率・履歴）を記録する

### 3.4 作文（Writing / 添削）

学習者のアウトプット（英作文）を支援する。3.1〜3.3 が受容・再認（読む・覚える・解く）中心
なのに対し、本機能は自分で英文を書き、その添削を受ける産出の練習を担う。

- ナビゲーションの「Writing」タブから利用する。`Lesson` に紐づかない独立した作文として書き溜める
- 入力は2つ: ①書いた英文 ②それに対応する日本語（英文の訳、または日本語での説明＝伝えたかった意図）
  - 日本語は「学習者が伝えたかった意図」を AI に渡し、添削の方向を確定させる（英文だけでは曖昧な
    修正を、意図に沿って直せる）
- 「Review」で英日両方を Claude API に送り、**修正後の英文＋日本語の解説**を受け取り画面に表示する
  - 修正英文の各語はタップで単語帳登録に繋げられる（3.2章の学習動線へ接続）
- **反復改善（ラウンド式の履歴スレッド）**: 添削は1回で終わらせず、フィードバックを踏まえて下書きを
  直し「Re-review」すると次のラウンドとして積み上がる。再添削時は過去の全ラウンド（英文・修正・解説）を
  AI に渡すため、「前回の指摘は直った／まだ残る問題はここ」と改善の推移を踏まえた添削になる
  - 画面上部にこれまでのラウンドを古い順で並べ、下部の下書きエディタから次のラウンドを送る
  - 添削後もエディタの英文は学習者が書いたまま維持し、自分で手直しして再送する（修正英文の自動投入はしない）
  - 下書きが最終ラウンドと同一のときは送る変更が無いため Review は無効
- 細かな指摘リスト／スコアは将来拡張（v1 は修正英文＋解説のみ）

### 3.5 音声（Audio 取り込み・文字起こし・翻訳）

授業で配布される音声（会話・リスニング教材など）を取り込み、写真の OCR・翻訳（3.1章）と同じ
学習動線に載せる。

- iOSの「ファイル」（Dropbox・iCloud・端末内）から音声を取り込み、アプリの正式データ（`AudioClip`）にする。
  取り込んだ音声はライブラリとして一覧・再生でき、0個以上のレッスンへ紐付けられる（レッスン非依存でも可）
- 詳細画面の**手動ボタン**で、英文の逐語文字起こし（transcript）＋日本語全訳を生成する
  （音声は長くコストも高いため、取り込み時の自動処理はせずクリップ単位でユーザーが実行する）
  - 文字起こしは音声入力対応の Gemini、英→日の翻訳は 3.1 と同じ Claude 経路を再利用する2段構成
  - v1 は短いクリップのみ対象（インライン送信の上限 ≈14MB を超えるものは事前に弾く）。
    長尺録音の分割・タイムスタンプ付き字幕同期は将来拡張
- 完了後は写真詳細（3.1）と同じ体験を提供する: 英文は単語タップで単語帳登録（3.2章の動線へ接続）、
  訳は Markdown 表示。音声由来のタップ登録は出現元として記録し、AI 単語情報生成に transcript を文脈として渡す
- データモデルは [data-model.md](data-model.md) §4.5 `AudioClip`。バックエンドは `POST /api/transcribe-translate`
  で文字起こし＋翻訳を実行し、通信ログ（コスト内訳）を管理画面に表示する（5.2章）。
  設計: [docs/plans/audio-transcription-translation.md](../plans/audio-transcription-translation.md)

## 4. データの管理単位・保存方針

- 「クラス」と、その下の「レッスン（授業）」という 2 段階の単位を持つ
  - クラス: 受講しているコース・科目単位（例:「ESL Beginner A」）。複数レッスンをまとめる親単位
  - レッスン: クラス内の1回分の授業。日付等による自動区切りはせず、作成・切り替えはユーザーが手動で行う
- 写真・単語・問題は、以下 2 つの表示モードで参照できる
  - レッスンごとのビュー
  - 全体の一覧ビュー
- ユーザーデータ（クラス・レッスン・写真・単語・問題・成績履歴）は**端末内ローカル保存のみ**
  - クラウド同期・バックアップは対象外（将来検討事項）

## 5. アーキテクチャ

### 5.1 iOS アプリ

- ネイティブ iOS（Swift / SwiftUI）
- ユーザーデータはローカル（端末内）に保存
- OCR・翻訳・問題生成は、すべてオンライン時にバックエンド経由で Claude API を呼び出す
  （オンデバイス OCR は採用しない）

### 5.2 バックエンド

- Claude API へのリクエストを中継するバックエンドを用意する（API キーをアプリに直接持たせない）
- まずはローカル開発環境で動作させる（クラウドデプロイは将来検討）
- 通信ログを記録し、管理画面（Web）で閲覧できるようにする
  - 表示内容: トークン数・コスト、画像と OCR 結果の対応、単語と翻訳の対応、生成された問題、
    作文の添削（英文・意図・修正英文・解説）

## 6. 言語設定

- 翻訳の言語ペア: 英語 ⇔ 学習者が設定画面で指定した母語
- 対応言語の範囲は MVP では未確定（実装フェーズで決定）

## 7. 開発フェーズ（優先順位）

1. **Phase 1**: 撮影 → OCR・翻訳
2. **Phase 2**: 単語帳（登録・翻訳・復習）
3. **Phase 3**: 問題作成
4. **Phase 4**: 作文（英作文の添削）

各 Phase ともに、対応するバックエンド機能・管理画面（通信ログ表示）を含めて実装する。

## 8. 対象外（Non-goals / 将来検討）

- クラウドへのユーザーデータ同期・複数端末利用
- バックエンドの本番クラウドデプロイ（まずはローカルのみ）
- 問題の難易度を手動調整する UI
- オフラインでの OCR・翻訳・問題生成（ネットワーク必須）

## 9. 未確定事項（実装フェーズで詰める）

- 対応言語（母語）の具体的なリスト
- ~~画面構成の詳細（ワイヤーフレーム）~~ → [screen-design.md](screen-design.md) で検討
- ~~データモデルの詳細スキーマ（Lesson / Photo / Word / Question / QuizResult 等）~~ → [data-model.md](data-model.md) で確定
- バックエンドの技術スタック（言語・フレームワーク）
- ~~単語帳の間隔反復アルゴリズムの詳細~~ → 固定ステップの Leitner 方式で確定
  （[data-model.md](data-model.md) §5、策定経緯: [docs/plans/archive/word-memorization-quiz.md](../plans/archive/word-memorization-quiz.md)）
